name: my_app
description: A new Flutter project

# 版本号配置
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: 1.0.4 # 使用当前版本
  url_launcher: 6.3.1
  flutter_inappwebview: 6.1.1
  permission_handler: 11.4.0 # 推荐但非必须
  dio: ^5.4.0
  path_provider: ^2.1.2
  device_info_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # 启动页资源配置
  assets:
    - assets/splash.png # 启动页图片路径
